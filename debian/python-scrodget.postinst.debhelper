# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/python-scrodget.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall python-scrodget
	if grep -qs '^python-scrodget$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^python-scrodget$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
